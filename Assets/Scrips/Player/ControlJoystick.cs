using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlJoystick : MonoBehaviour
{
    // Start is called before the first frame update

/*
   float horizontalMove = 0;
    float verticalMove = 0;

    public float runnSpeedHorizontal = 3;
    public float runnSpeedVertical = 3;
    public float runnSpeed = 0;

    private static Rigidbody2D playerBody;

    public Joystick joystick;

    void Start()
    {
        playerBody = GetComponent<Rigidbody2D>();
    }

    // Update is called once per frame
    void Update()
    {
        verticalMove = joystick.verticalMove * runnSpeedVertical;
        horizontalMove = joystick.horizontalMove * runnSpeedHorizontal;
        transform.position += new Vector3(horizontalMove, verticalMove,0) * Time.deltaTime * runnSpeed;
    }*/
}
